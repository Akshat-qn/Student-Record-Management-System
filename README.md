# Student-Record-Management-System
Student Record Management System is a Python-based desktop appli. built using Tkinter and MySQL. It performs secure CRUD operations through parameterized SQL queries and displays records dynamically using ttk.Treeview. Designed with an obj-oriented structure, it ensures modular database connectivity, input validation, and robust exception handling.
